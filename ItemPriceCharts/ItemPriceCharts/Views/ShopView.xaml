<UserControl x:Class="ItemPriceCharts.UI.WPF.Views.ShopView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             xmlns:local="ItemPriceCharts.clr-namespace:UI.WPF.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:cv="clr-namespace:ItemPriceCharts.UI.WPF.Converters"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             mc:Ignorable="d"
             Height="550"
             Width="850">
    <Grid Background="#5e81ff">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="50" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <Grid Margin="0 8 0 0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <ListView ItemsSource="{Binding Path=OnlineShops}"
                          Style="{StaticResource Styles.ListView}"
                          Name="OnlineShopsListView">

                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="MouseDoubleClick">
                            <i:InvokeCommandAction Command="{Binding ShowItemsCommand}"
                                                   CommandParameter="{Binding ElementName=OnlineShopsListView, Path=SelectedItem}"/>
                        </i:EventTrigger>
                    </i:Interaction.Triggers>

                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Text="{Binding}"/>
                                <!--<TextBlock Text="Description"/>-->
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>

            <Grid Grid.Row="2"
                  VerticalAlignment="Bottom"
                  Margin="5 0 0 0">
                <StackPanel Orientation="Horizontal">
                    <Button Style="{StaticResource Styles.MenuButtons}"
                            HorizontalAlignment="Left"
                            Command="{Binding ShowCreateShopCommand}"
                            Margin="5 10"
                            Content="Add a new shop"/>
                    <Button Style="{StaticResource Styles.MenuButtons}"
                            HorizontalAlignment="Right"
                            Command="{Binding ShowDeleteShopCommand}"
                            Margin="5 10"
                            Content="Delete shop"/>
                </StackPanel>
            </Grid>
        </Grid>

        <Grid Grid.Column="2"
              Visibility="{Binding ShowItems, Converter={cv:BoolToVisibilityConverter}}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Orientation="Horizontal"
                        Margin="0 20 0 10">
                <Button Name="ShowAddItem"
                        HorizontalAlignment="Left"
                        Width="35"
                        Height="35"
                        Style="{StaticResource Styles.Simple.Button}"
                        Foreground="White"
                        ToolTip="Add Item"
                        Command="{Binding ShowAddItemCommand}">
                    <materialDesign:PackIcon Kind="Add"
                                             Width="30"
                                             Height="30"/>
                </Button>
            </StackPanel>

            <ListView ItemsSource="{Binding Path=ItemsList}"
                      Grid.Row="1"
                      Style="{StaticResource Styles.ListView}"
                      Width="400"
                      Name="ItemsListView">

                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="MouseDoubleClick">
                        <i:InvokeCommandAction Command="{Binding ShowItemDetails}"
                                               CommandParameter="{Binding ElementName=ItemsListView, Path=SelectedItem}"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>

                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Text="{Binding}"/>
                            <!--<TextBlock Text="Description"/>-->
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>

        <!--<Grid Grid.Column="1"
              Margin="15"
              Visibility="{Binding IsChartShown, Converter={cv:BoolToVisibilityConverter}}">
            <lvc:CartesianChart Series="{Binding SeriesCollection}" 
                                LegendLocation="Right" >
                <lvc:CartesianChart.AxisY>
                    <lvc:Axis Title="Sales"
                              LabelFormatter="{Binding YFormatter}" />
                </lvc:CartesianChart.AxisY>
                <lvc:CartesianChart.AxisX>
                    <lvc:Axis Title="Month"
                              Labels="{Binding Labels}" />
                </lvc:CartesianChart.AxisX>
            </lvc:CartesianChart>
        </Grid>-->
    </Grid>
</UserControl>
